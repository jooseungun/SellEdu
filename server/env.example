# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=selledu

# JWT 설정
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# 서버 설정
PORT=3000
NODE_ENV=development
API_VERSION=v1

# CORS 설정 (필요시)
# CORS_ORIGIN=https://selledu.pages.dev

