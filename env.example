# Cloudflare D1 Database Environment Variables
# 이 파일을 복사하여 .env.local 파일을 만들고 실제 값으로 채워주세요

# ============================================
# Cloudflare D1 Database 설정
# ============================================
# D1 데이터베이스 ID (Cloudflare 대시보드에서 확인)
# Workers & Pages > D1 > 데이터베이스 선택 > Settings > Database ID
D1_DATABASE_ID=YOUR_D1_DATABASE_ID

# D1 데이터베이스 이름
D1_DATABASE_NAME=selledu-db

# ============================================
# JWT 인증 설정
# ============================================
# JWT 토큰 서명용 시크릿 키 (랜덤 문자열 권장)
# 예: openssl rand -hex 32
JWT_SECRET=your-secret-key-change-in-production

# JWT 토큰 만료 시간 (기본: 7일)
JWT_EXPIRES_IN=7d

# ============================================
# API 설정
# ============================================
# API 기본 URL (프로덕션에서는 자동 설정됨)
REACT_APP_API_URL=

# ============================================
# 개발 환경 설정
# ============================================
# 개발 모드 여부
NODE_ENV=development

# ============================================
# Cloudflare Pages Functions 설정
# ============================================
# Functions에서 사용할 환경변수는 Cloudflare 대시보드에서 설정해야 합니다
# Pages > 프로젝트 > Settings > Environment variables

